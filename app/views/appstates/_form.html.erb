<%= form_for(appstate) do |f| %>
  <% if appstate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(appstate.errors.count, "error") %> prohibited this appstate from being saved:</h2>

      <ul>
      <% appstate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
